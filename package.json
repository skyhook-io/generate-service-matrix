{
  "name": "generate-service-matrix",
  "version": "1.0.0",
  "description": "GitHub Action that generates a service × environment matrix from Skyhook or Koala configuration",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "jest"
  },
  "keywords": [
    "github-action",
    "deployment",
    "matrix",
    "monorepo"
  ],
  "author": "Skyhook",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "jest": "^29.7.0"
  }
}
